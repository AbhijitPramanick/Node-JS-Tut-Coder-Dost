// const sum = (a, b) => a + b;

// const diff = (a, b) => a - b;

// export { sum, diff };
const sum = function (a, b) {
    return a + b;
}
const diff = function (a, b) {
    return a - b;
}
export { sum, diff };